package phenote.matrix.model;

public interface MatrixCell {
  /*
   *  This is currently just a placeholder interface to maintain consistency.
   *  The implementation is in PhenotypeMatrixCell.java.
   *  This interface may be expanded if additional methods are ever needed, but
   *  for now, none are necessary.
   * 
   */
  
}
